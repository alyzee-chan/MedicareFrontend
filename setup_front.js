// Script de vérification de l'environnement MediCare+ Frontend
const config = {
    api_url: "http://localhost:5000",
    version: "1.0.0",
    status: "ready"
};

console.log("MediCare+ Frontend est prêt.");
console.log("Connexion à l'API :", config.api_url);
console.log("Version actuelle :", config.version);